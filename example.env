# ==============================================================================
# 🌍 myOS ALL-IN-ONE GIS STACK CONFIGURATION
# ==============================================================================
# Dieses File steuert alle Aspekte deines GIS-Servers. 
# Änderungen hier werden nach einem 'docker-compose up -d' wirksam.

# --- 🏗️ CONTAINER SETUP ---
PROJECT_NAME=myos
CONTAINER_NAME=myOS
# Das Root-Passwort für den SSH-Zugriff (User: root)
ROOT_PASSWORD=your_secure_password
# Zeitzone für System, Datenbank und Logs
TZ=Europe/Berlin

# --- 🌐 NETZWERK & PORTS ---
# Hier definierst du, auf welchen Ports dein Mac den Container erreicht.
# Format: HOST_PORT (dein Mac) -> INTERNER_PORT (Container)

# SSH Zugriff (Standard: 2225)
SSH_PORT=2225
# QGIS im Web-Browser (noVNC) (Standard: 8081)
VNC_PORT=8081
# pgAdmin4 Web-Interface (Standard: 5050)
PGADMIN_PORT=5050
# PostGIS Datenbank-Port (Standard: 5432)
DB_PORT=5432

# --- 🐘 DATENBANK (PostGIS 18) ---
DB_USER=admin
DB_PASSWORD=your_db_password
DB_NAME=myos_gis

# --- 🛡️ PGADMIN4 SETTINGS ---
# Login-Daten für die Weboberfläche von pgAdmin
PGADMIN_DEFAULT_EMAIL=admin@admin.com
PGADMIN_DEFAULT_PASSWORD=your_pgadmin_password

# --- 🖥️ GRAFIK & PERFORMANCE ---
# Auflösung des virtuellen Desktops (Breite x Höhe x Farbtiefe)
RESOLUTION=2560x1440x24
# Skalierung der Benutzeroberfläche (1.0 = normal, 1.5 = groß/HiDPI)
UI_SCALE=1.5
# Sprache der Benutzeroberfläche (de, en, etc.)
SYSTEM_LANG=de
